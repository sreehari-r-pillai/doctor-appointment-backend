import { Body, Controller, Get, Param, Post, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../common/guards/jwt-auth.guard'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { AvailabilityService } from './availability.service'; @ApiTags('availability') @ApiBearerAuth() @UseGuards(JwtAuthGuard, RolesGuard) @Controller('availability') export class AvailabilityController{ constructor(private svc:AvailabilityService){} @Post('upsert') @Roles('ADMIN','DOCTOR') upsert(@Body('doctorId') doctorId:string, @Body('date') date:string, @Body('slot') slot:string, @Req() req:any){ return this.svc.upsert(doctorId,date,slot,req.user);} @Get(':doctorId') @Roles('ADMIN','DOCTOR','PATIENT') list(@Param('doctorId') doctorId:string){ return this.svc.listForDoctor(doctorId);} }