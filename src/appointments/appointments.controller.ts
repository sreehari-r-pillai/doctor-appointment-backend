import { Body, Controller, Delete, Get, Param, Patch, Post, Req, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../common/guards/jwt-auth.guard'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { AppointmentsService } from './appointments.service'; import { CreateAppointmentDto } from './dto/create-appointment.dto'; @ApiTags('appointments') @ApiBearerAuth() @UseGuards(JwtAuthGuard, RolesGuard) @Controller('appointments') export class AppointmentsController{ constructor(private svc:AppointmentsService){} @Post() @Roles('ADMIN','DOCTOR','PATIENT') create(@Body() dto:CreateAppointmentDto, @Req() req:any){ return this.svc.create(dto, req.user);} @Get('me') @Roles('ADMIN','DOCTOR','PATIENT') list(@Req() req:any){ return this.svc.listMine(req.user);} @Patch(':id/reschedule') @Roles('ADMIN','DOCTOR','PATIENT') reschedule(@Param('id') id:string, @Body('startTime') startTime:string, @Req() req:any){ return this.svc.reschedule(id,startTime,req.user);} @Delete(':id') @Roles('ADMIN','DOCTOR','PATIENT') cancel(@Param('id') id:string, @Req() req:any){ return this.svc.cancel(id, req.user);} }