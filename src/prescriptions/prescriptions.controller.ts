import { Body, Controller, Post, Req, UploadedFile, UseGuards, UseInterceptors } from '@nestjs/common'; import { FileInterceptor } from '@nestjs/platform-express'; import { ApiBearerAuth, ApiConsumes, ApiTags } from '@nestjs/swagger'; import { JwtAuthGuard } from '../common/guards/jwt-auth.guard'; import { Roles } from '../common/decorators/roles.decorator'; import { RolesGuard } from '../common/guards/roles.guard'; import { prescriptionStorage, fileFilter } from './upload/multer.config'; @ApiTags('prescriptions') @ApiBearerAuth() @UseGuards(JwtAuthGuard, RolesGuard) @Controller('prescriptions') export class PrescriptionsController{ @Post('upload') @ApiConsumes('multipart/form-data') @Roles('ADMIN','DOCTOR') @UseInterceptors(FileInterceptor('file',{ storage:prescriptionStorage, limits:{ fileSize: parseInt(process.env.MAX_FILE_SIZE||'5242880') }, fileFilter })) upload(@UploadedFile() file:Express.Multer.File, @Body() body:any, @Req() req:any){ return { ok:true, file: { name:file.filename, size:file.size }, meta: body, by: req.user }; }}